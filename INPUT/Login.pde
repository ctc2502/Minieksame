class Login {

  void loginLaerer() {
    background(bgColor);
    fill(255);
    textAlign(CENTER);
    text("Login", 400, 75);
    textAlign(0);
    for (InputField t : textboxes) {
      t.DRAW();
    }
  }

  void loginElev() {
    background(bgColor);
    fill(255);
    textAlign(CENTER);
    text("Login", 400, 75);
    textAlign(0);
    for (InputField t : textboxes) {
      t.DRAW();
    }
  }
}
